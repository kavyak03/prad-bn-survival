[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prad-bn"
version = "0.1.0"
description = "Toy repo: iterative Bayesian network survival strata prediction (PRAD-like)"
requires-python = ">=3.9"
